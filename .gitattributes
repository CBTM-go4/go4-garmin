# Normalise line endings so the repo is consistent across Windows/macOS/Linux.
* text=auto

# Shell scripts must be LF or they won't run on macOS/Linux.
*.sh text eol=lf

# PowerShell is happiest with CRLF on Windows.
*.ps1 text eol=crlf
